import 'package:flutter/material.dart';
class NamePrinter extends StatelessWidget {
  const NamePrinter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Etukudo Emmanuel"),
    );
  }
}
